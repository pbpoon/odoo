<?xml version="1.0"?>
<odoo>
    <record id="mail_activity_type_view_form" model="ir.ui.view">
        <field name="name">mail.activity.type.view.form</field>
        <field name="model">mail.activity.type</field>
        <field name="arch" type="xml">
            <form string="Activities">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button class="oe_stat_button" type="object" name="toggle_active" icon="fa-archive">
                            <field name="active" widget="boolean_button" options='{"terminology": "archive"}'/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <label for="name" class="oe_edit_only"/>
                        <h1><field name="name"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="category" invisible="1"/>
                            <field name="res_model_id" groups="base.group_no_one"/>
                            <field name="summary"/>
                            <field name="icon" groups="base.group_no_one"/>
                            <label for="days"/>
                            <div>
                                <field name="days" class="oe_inline"/>
                                <span class="oe_inline">days</span>
                            </div>
                        </group>
                        <group>
                            <field name="next_type_ids" widget="many2many_tags"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="mail_activity_type_view_search" model="ir.ui.view">
        <field name="name">mail.activity.type.search</field>
        <field name="model">mail.activity.type</field>
        <field name="arch" type="xml">
            <search string="Activities">
                <field name="name"/>
                <filter string="Archived" domain="[('active', '=', False)]" name="archived"/>
            </search>
        </field>
    </record>

    <record id="mail_activity_type_view_tree" model="ir.ui.view">
        <field name="name">mail.activity.type.view.tree</field>
        <field name="model">mail.activity.type</field>
        <field name="arch" type="xml">
            <tree string="Activities">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="summary"/>
                <field name="days"/>
                <field name="res_model_id" groups="base.group_no_one"/>
                <field name="icon" groups="base.group_no_one"/>
            </tree>
        </field>
    </record>

    <record id="mail_activity_type_action" model="ir.actions.act_window">
        <field name="name">Activity Types</field>
        <field name="res_model">mail.activity.type</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem
      id="menu_mail_activity_type"
      action="mail_activity_type_action"
      parent="base.menu_email"
    />

    <record id="mail_activity_view_form_popup" model="ir.ui.view">
        <field name="name">mail.activity.view.form.popup</field>
        <field name="model">mail.activity</field>
        <field name="priority">20</field>
        <field name="arch" type="xml">
            <form string="Log an Activity" create="false">
                <sheet string="Activity">
                    <group invisible="1">
                        <field name="activity_category" invisible="1" />
                        <field name="res_model" invisible="1"/>
                        <field name="res_model_id" invisible="1"/>
                        <field name="res_id" invisible="1"/>
                        <field name="previous_activity_type_id"/>
                        <field name="has_recommended_activities"/>
                    </group>
                    <group attrs="{'invisible': [('has_recommended_activities','=',False)]}">
                        <div>
                            <p><strong>Recommended Activities</strong></p>
                            <field name="recommended_activity_type_id" widget="radio"
                                domain="[('previous_type_ids', '=', previous_activity_type_id)]"
                                options="{'horizontal':true}"
                                nolabel="1"/>
                        </div>
                    </group>
                    <group>
                        <group>
                            <field name="activity_type_id" required="1" options="{'no_create': True, 'no_open': True}"/>
                            <field name="summary" placeholder="e.g. Discuss proposal"/>
                        </group>
                        <group>
                            <field name="date_deadline"/>
                            <field name="user_id"/>
                        </group>
                    </group>
                    <field name="note" placeholder="Log a note..."/>
                    <footer>
                        <button string="Schedule" name="action_close_dialog"
                            type="object" class="btn-primary"/>
                        <button string="Mark as Done" name="action_done"
                            type="object" class="btn-default"/>
                        <button string="Done &amp; Schedule Next" name="action_done_schedule_next" type="object" class="btn-default"/>
                        <button string="Discard" class="btn-default" special="cancel" />
                    </footer>
                </sheet>
            </form>
        </field>
    </record>

    <record id="mail_activity_view_search" model="ir.ui.view">
        <field name="name">mail.activity.view.search</field>
        <field name="model">mail.activity</field>
        <field name="arch" type="xml">
            <search string="Activity">
                <field name="res_model_id"/>
                <field name="summary"/>
                <field name="activity_type_id"/>
                <separator/>
                <filter string="My Activities" name="activities_my"
                        domain="[('create_uid', '=', uid)]"/>
                <separator/>
                <filter string="Late Activities" name="activities_overdue"
                        domain="[('date_deadline', '&lt;', context_today().strftime('%Y-%m-%d'))]"
                        help="Show all records which has next action date is before today"/>
                <filter string="Today Activities" name="activities_today"
                        domain="[('date_deadline', '=', context_today().strftime('%Y-%m-%d'))]"/>
                <filter string="Future Activities" name="activities_upcoming_all"
                        domain="[('activity_ids.date_deadline', '&gt;', context_today().strftime('%Y-%m-%d'))
                        ]"/>
                <separator/>
                <group expand="0" string="Group By">
                    <filter string="Deadline" name="date_deadline" context="{'group_by': 'date_deadline'}"/>
                    <filter string="Created By" name="createdby" context="{'group_by': 'create_uid'}"/>
                    <filter string="Activity Type" name="activittype" context="{'group_by': 'activity_type_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="mail_activity_view_tree" model="ir.ui.view">
        <field name="name">mail.activity.view.tree</field>
        <field name="model">mail.activity</field>
        <field name="arch" type="xml">
            <tree string="Next Activities"
                    decoration-danger="date_deadline &lt; current_date"
                    decoration-success="date_deadline == current_date"
                    default_order="date_deadline" create="false">
                <field name="res_name"/>
                <field name="activity_type_id"/>
                <field name="summary"/>
                <field name="date_deadline"/>
            </tree>
        </field>
    </record>

    <record id="mail_activity_view_calendar" model="ir.ui.view">
        <field name="name">mail.activity.view.calendar</field>
        <field name="model">mail.activity</field>
        <field name="priority" eval="2"/>
        <field name="arch" type="xml">
            <calendar string="Activity" date_start="date_deadline" color="activity_type_id">
                <field name="res_name"/>
                <field name="create_uid"/>
                <field name="summary"/>
            </calendar>
        </field>
    </record>

    <record id="mail_activity_reminder_view_form" model="ir.ui.view">
        <field name="name">mail.activity.view.form</field>
        <field name="model">mail.activity</field>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <form string="Reminders">
                <sheet>
                    <field name="reminder_open" invisible="1"/>
                    <div class="oe_button_box" name="button_box">
                        <button class="oe_stat_button" icon="fa-check" type="object" name="action_reminder_close" string="Mark as Done" attrs="{'invisible' : [('reminder_open', '=', False)]}" />
                        <button class="oe_stat_button" icon="fa-undo" type="object" name="action_reminder_open" string="Re-open" attrs="{'invisible' : [('reminder_open', '=', True)]}" />
                    </div>
                    <div class="oe_title">
                        <label for="summary" class="oe_edit_only"/>
                        <h1><field name="summary"/></h1>
                    </div>
                    <group>
                        <field name="date_deadline" string="Reminder Date"/>
                    </group>
                    <field name="note"/>
                </sheet>
            </form>
        </field>
    </record>

    <record id="mail_activity_view_kanban" model="ir.ui.view">
        <field name="name">mail.activity.view.kanban</field>
        <field name="model">mail.activity</field>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_small_column">
                <field name="summary"/>
                <field name="note"/>
                <field name="reminder_open"/>
                <field name="res_id"/>
                <field name="res_model"/>
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="oe_kanban_global_click">
                            <div class="o_dropdown_kanban dropdown">
                                <a class="dropdown-toggle btn" data-toggle="dropdown" href="#">
                                    <span class="fa fa-ellipsis-v"/>
                                </a>
                                <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
                                    <li><a type="delete">Delete</a></li>
                                </ul>
                            </div>
                            <div class="oe_kanban_content">
                                <div>
                                    <div t-attf-class="#{record.reminder_open.raw_value ? '' : 'o_text_line_through'}">
                                        <strong><field name="summary"/></strong>
                                        <p class="o_activity_reminder_kanban_content"><t t-esc="_.str.stripTags(record.note.raw_value)"/></p>
                                    </div>
                                    <span t-if="!record.res_id.raw_value and !record.res_model.raw_value">
                                        <a name="action_reminder_close" type="object" t-if="record.reminder_open.raw_value" title="Mark as done"><i class="fa fa-check"/></a>
                                        <a name="action_reminder_open" type="object" t-if="!record.reminder_open.raw_value" title="Re-open reminder"><i class="fa fa-undo"/></a>
                                    </span>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="mail_activity_reminder_view_search" model="ir.ui.view">
        <field name="name">mail.activity.reminder.view.search</field>
        <field name="model">mail.activity</field>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <search string="Activity">
                <field name="reminder_open"/>
                <separator/>
                <filter string="Active Reminders" name="filter_open_reminders"
                        domain="[('reminder_open', '=', True)]"/>
                <filter string="Closed Reminders" name="filter_closed_reminders"
                        domain="[('reminder_open', '=', False)]"/>
                <separator/>
            </search>
        </field>
    </record>

     <record id="mail_activity_reminders_action" model="ir.actions.act_window">
        <field name="name">Reminders</field>
        <field name="res_model">mail.activity</field>
        <field name="view_type">form</field>
        <field name="search_view_id" ref="mail_activity_reminder_view_search"/>
        <field name="view_ids"
                   eval="[(5, 0, 0),
                          (0, 0, {'view_mode': 'kanban', 'view_id': ref('mail_activity_view_kanban')}),
                          (0, 0, {'view_mode': 'form', 'view_id': ref('mail_activity_reminder_view_form')})]"/>
        <field name="view_id" ref="mail.mail_activity_view_kanban"/>
        <field name="view_mode">kanban,form</field>
        <field name="domain">[('user_id', '=', uid), ('res_id', '=', False), ('res_model', '=', False)]</field>
        <field name="context" eval="{'search_default_filter_open_reminders': 1}"/>
    </record>
</odoo>
